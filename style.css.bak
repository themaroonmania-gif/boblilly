/* ============================================================
   Bob Lilly Promotions — Premium Redesign
   Aesthetic: Dark Mode, Glassmorphism, Neon Glow, Premium Corporate
   ============================================================ */

@import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800;900&family=Inter:wght@400;500;600;700&display=swap');

:root {
  --navy: #050B14;
  --navy-2: #0A1628;
  --navy-3: #112540;
  --orange: #FF552F;
  --orange-glow: rgba(255, 85, 47, 0.4);
  --blue: #0072D2;
  --white: #FFFFFF;
  --text-muted: #94A3B8;
  --glass-bg: rgba(255, 255, 255, 0.03);
  --glass-border: rgba(255, 255, 255, 0.08);
  --display: 'Outfit', sans-serif;
  --text: 'Inter', sans-serif;
  --ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);
  --radius: 24px;
}

* { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; background: var(--navy); }
body {
  font-family: var(--text);
  color: var(--white);
  background-color: var(--navy);
  line-height: 1.6;
  overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
}
img { max-width: 100%; display: block; }
a { text-decoration: none; color: inherit; transition: color 0.3s; }
ul { list-style: none; }

/* Animated Mesh Gradient Background */
.mesh-bg {
  position: fixed;
  top: 0; left: 0; width: 100vw; height: 100vh;
  z-index: -1;
  background: 
    radial-gradient(circle at 15% 50%, rgba(255, 85, 47, 0.15), transparent 50%),
    radial-gradient(circle at 85% 30%, rgba(0, 114, 210, 0.15), transparent 50%),
    var(--navy);
  filter: blur(80px);
  animation: mesh-pulse 15s ease-in-out infinite alternate;
}
@keyframes mesh-pulse {
  0% { transform: scale(1); }
  100% { transform: scale(1.1); }
}

.container { width: min(1280px, 92vw); margin: 0 auto; position: relative; z-index: 1; }
.section { padding: 120px 0; position: relative; }

/* Typography */
h1, h2, h3, h4, h5 { font-family: var(--display); font-weight: 800; letter-spacing: -0.02em; line-height: 1.1; }
h1 { font-size: clamp(3rem, 6vw, 5.5rem); margin-bottom: 24px; text-transform: uppercase; }
h2 { font-size: clamp(2.5rem, 4vw, 3.5rem); margin-bottom: 20px; }
.text-gradient {
  background: linear-gradient(135deg, #fff, #94A3B8);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
.text-orange { color: var(--orange); }

/* Buttons */
.btn {
  display: inline-flex; align-items: center; justify-content: center;
  gap: 12px; font-family: var(--display); font-weight: 700; font-size: 1.1rem;
  padding: 16px 32px; border-radius: 100px;
  transition: all 0.4s var(--ease-out-expo);
  cursor: pointer; position: relative; overflow: hidden;
}
.btn-primary {
  background: var(--orange); color: var(--white);
  box-shadow: 0 0 20px var(--orange-glow);
}
.btn-primary:hover {
  transform: translateY(-3px);
  box-shadow: 0 10px 30px var(--orange-glow);
  background: #ff6a4a;
}
.btn-glass {
  background: var(--glass-bg); border: 1px solid var(--glass-border);
  backdrop-filter: blur(10px); color: var(--white);
}
.btn-glass:hover {
  background: rgba(255,255,255,0.1); transform: translateY(-3px);
}

/* Glassmorphism Cards */
.glass-card {
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  backdrop-filter: blur(20px);
  border-radius: var(--radius);
  padding: 40px;
  transition: all 0.4s var(--ease-out-expo);
  position: relative;
  overflow: hidden;
}
.glass-card::before {
  content: ''; position: absolute; top: 0; left: -100%; width: 50%; height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.05), transparent);
  transform: skewX(-20deg); transition: 0.7s;
}
.glass-card:hover {
  transform: translateY(-10px);
  border-color: rgba(255, 85, 47, 0.3);
  box-shadow: 0 20px 40px rgba(0,0,0,0.5);
}
.glass-card:hover::before { left: 150%; }

/* Navigation */
.nav {
  position: fixed; top: 0; width: 100%; z-index: 1000;
  padding: 24px 0; transition: all 0.4s;
}
.nav.scrolled {
  padding: 16px 0;
  background: rgba(5, 11, 20, 0.8);
  backdrop-filter: blur(20px);
  border-bottom: 1px solid var(--glass-border);
}
.nav-inner { display: flex; justify-content: space-between; align-items: center; }
.nav-logo img { height: 48px; }
.nav-links { display: flex; gap: 32px; }
.nav-links a { font-family: var(--display); font-weight: 600; font-size: 1.1rem; opacity: 0.8; }
.nav-links a:hover { opacity: 1; color: var(--orange); }

/* Hero Section */
.hero { min-height: 100vh; display: flex; align-items: center; padding-top: 120px; }
.hero-content { max-width: 800px; }
.hero-tag {
  display: inline-block; padding: 8px 16px; border-radius: 100px;
  background: rgba(255, 85, 47, 0.1); border: 1px solid rgba(255, 85, 47, 0.2);
  color: var(--orange); font-family: var(--display); font-weight: 700; font-size: 0.9rem;
  margin-bottom: 24px; text-transform: uppercase; letter-spacing: 2px;
}
.hero-desc { font-size: 1.25rem; color: var(--text-muted); margin-bottom: 40px; max-width: 600px; }
.hero-actions { display: flex; gap: 20px; align-items: center; }

/* Marquee */
.marquee-container {
  padding: 40px 0; border-top: 1px solid var(--glass-border); border-bottom: 1px solid var(--glass-border);
  overflow: hidden; white-space: nowrap; position: relative;
  background: rgba(0,0,0,0.2);
}
.marquee-track {
  display: inline-block; animation: scroll 30s linear infinite;
}
.marquee-item {
  display: inline-flex; align-items: center; gap: 60px; padding-right: 60px;
}
.marquee-item img { height: 40px; filter: grayscale(1) brightness(2); opacity: 0.5; transition: 0.3s; }
.marquee-item img:hover { filter: grayscale(0) brightness(1); opacity: 1; }
@keyframes scroll { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }

/* Services Grid */
.services-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 30px; margin-top: 60px; }
.service-icon {
  width: 64px; height: 64px; border-radius: 16px; background: rgba(255, 85, 47, 0.1);
  display: flex; align-items: center; justify-content: center; margin-bottom: 24px;
  color: var(--orange); font-size: 2rem; border: 1px solid rgba(255, 85, 47, 0.2);
}
.service-title { font-size: 1.5rem; margin-bottom: 12px; }
.service-desc { color: var(--text-muted); font-size: 1rem; }

/* Case Studies */
.case-card {
  display: grid; grid-template-columns: 1fr 1fr; gap: 40px; align-items: center;
  margin-bottom: 60px; background: var(--glass-bg); border-radius: var(--radius);
  border: 1px solid var(--glass-border); overflow: hidden;
}
.case-card:nth-child(even) { grid-template-columns: 1fr 1fr; direction: rtl; }
.case-card:nth-child(even) > * { direction: ltr; }
.case-image { width: 100%; height: 400px; object-fit: cover; }
.case-content { padding: 40px; }
.case-tag { color: var(--orange); font-family: var(--display); font-weight: 700; margin-bottom: 16px; display: block; }
.case-title { font-size: 2.5rem; margin-bottom: 20px; }

/* Reveal Animations */
.reveal { opacity: 0; transform: translateY(40px); transition: all 1s var(--ease-out-expo); }
.reveal.active { opacity: 1; transform: translateY(0); }
.delay-1 { transition-delay: 0.1s; }
.delay-2 { transition-delay: 0.2s; }
.delay-3 { transition-delay: 0.3s; }

/* Footer */
footer { background: #02050A; padding: 80px 0 40px; border-top: 1px solid var(--glass-border); }
.footer-grid { display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 60px; margin-bottom: 60px; }
.footer-logo { max-width: 200px; margin-bottom: 24px; }
.footer-text { color: var(--text-muted); }
.footer-link { display: block; color: var(--text-muted); margin-bottom: 12px; transition: 0.3s; }
.footer-link:hover { color: var(--orange); transform: translateX(5px); }

@media (max-width: 900px) {
  .hero-content { text-align: center; margin: 0 auto; }
  .hero-actions { justify-content: center; }
  .nav-links { display: none; }
  .case-card, .case-card:nth-child(even) { grid-template-columns: 1fr; direction: ltr; }
  .footer-grid { grid-template-columns: 1fr; gap: 40px; }
}
